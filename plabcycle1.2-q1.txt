def word_count(str):
	words=str.split()
	count=dict()
	for word in words:
		if word in counts:
			counts[word]=counts[word]+1
		else:
			counts[word]=1
	return counts
print(word_count("Be a good person))




output:
{'always': 1, 'good': 1, 'Always': 1, 'be': 1, 'person': 1, 'a': 1}